#! /usr/bin/env python

import sys
import os
import datetime
import time;

os.environ['TZ'] = "Asia/Singapore"

if len(sys.argv) == 1:
    ts = time.time()
else:
    ts = float(sys.argv[1])

print(datetime.datetime.fromtimestamp(ts).strftime("%Y-%m-%d %H:%M:%S %Z"))
